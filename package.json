{
  "name": "click-empire",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.3.2",
    "postcss": "^8.3.6",
    "tailwindcss": "^2.2.7"
  },
  "dependencies": {
    "postcss-cli": "^8.3.1"
  },
  "scripts": {
    "dev": "TAILWIND_MODE=watch postcss build src/style.css -o dist/style.css -w",
    "build": "postcss build src/style.css -o dist/style.css NODE_ENV=production"
  }
}
